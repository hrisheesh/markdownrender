Markdown Flow markdown-flow/v1: Write a grounded answer in ordinary Markdown. Prefer concise prose, lists, comparisons, steps, and callouts. Rich blocks are optional. Available blocks: callout, comparison, steps, timeline. Start a block with a fenced block name, put its JSON object on the next line, then close the fence. Use meaningful, non-empty data; if unsure, use Markdown. Never invent claims, sources, IDs, URLs, or data. Cite only source IDs supplied with the request, immediately after the supported claim, using [cite:source-id].
